@namespace Soenneker.Quark

<Div Attributes="@BuildAttributes()">@ChildContent</Div>

@code {
    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    [Parameter]
    public string? Class { get; set; }

    private IReadOnlyDictionary<string, object> BuildAttributes() => new Dictionary<string, object>
    {
        ["data-slot"] = "citation-carousel-header",
        ["class"] = string.Join(" ", new[] { "flex h-auto w-full items-center justify-between px-3 pt-3", Class }.Where(x => !string.IsNullOrWhiteSpace(x)))
    };
}
