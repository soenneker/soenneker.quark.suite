@namespace Soenneker.Quark
@inherits Soenneker.Quark.Element
@implements ICardHeader

<Div Attributes="@BuildAttributes()">
    @ChildContent
</Div>

@code {
    protected override void ApplyDefaultParameters()
    {
        base.ApplyDefaultParameters();
        DataSlot ??= "card-header";
        Display ??= Quark.Display.Grid;
        ItemsAlign ??= Items.Start;
        Gap ??= Quark.Gap.Is1;
        Padding ??= Quark.Padding.OnX.Is4.Modifier("group-data-[size=sm]/card").OnX.Is3;
    }

    protected override void BuildAttributesCore(Dictionary<string, object> attributes)
    {
        base.BuildAttributesCore(attributes);

        PrependClassAttribute(attributes, "group/card-header @container/card-header auto-rows-min rounded-t-xl has-data-[slot=card-action]:grid-cols-[1fr_auto] has-data-[slot=card-description]:grid-rows-[auto_auto] [.border-b]:pb-4 group-data-[size=sm]/card:[.border-b]:pb-3");
    }
}
