@namespace Soenneker.Quark
@inherits Soenneker.Quark.Element
@implements IDialogFooter

<div @attributes="BuildAttributes()">
    @ChildContent
</div>

@code {
    protected override void ApplyDefaultParameters()
    {
        base.ApplyDefaultParameters();

        Display ??= Quark.Display.Flex;
        Gap ??= Quark.Gap.Is2;
        Rounded ??= Quark.Rounded.Bottom.Xl;
        Border ??= Quark.Border.Default.FromTop;
        BackgroundColor ??= Quark.BackgroundColor.Utility("bg-muted/50");
        Padding ??= Quark.Padding.Is4;
    }

    protected override void BuildAttributesCore(Dictionary<string, object> attributes)
    {
        base.BuildAttributesCore(attributes);
        attributes["data-slot"] = "dialog-footer";
        // shadcn DialogFooter - base ui/dialog.tsx
        BuildClassAttribute(attributes, (ref cls) =>
        {
            AppendClass(ref cls, "-mx-4 -mb-4 flex-col-reverse sm:flex-row sm:justify-end");
        });
    }
}
