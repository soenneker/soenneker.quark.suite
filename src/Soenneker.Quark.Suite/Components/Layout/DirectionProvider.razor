@using Soenneker.Bradix

@namespace Soenneker.Quark
@inherits Soenneker.Quark.Element

<CascadingValue Name="DirectionProviderDir" Value="@DirValue">
    <BradixDirectionProvider Dir="@DirValue">
        @ChildContent
    </BradixDirectionProvider>
</CascadingValue>

@code {
    /// <summary>
    /// Text direction: "ltr" or "rtl".
    /// Matches Radix DirectionProvider direction prop.
    /// </summary>
    [Parameter]
    public string Dir { get; set; } = "ltr";

    /// <summary>
    /// Text direction: "ltr" or "rtl".
    /// </summary>
    [Parameter]
    public string? Direction { get; set; }

    private string DirValue => ResolveDirValue();

    private string ResolveDirValue()
    {
        ReadOnlySpan<char> value = (Direction ?? Dir).AsSpan().Trim();
        return value.Equals("rtl", StringComparison.OrdinalIgnoreCase) ? "rtl" : "ltr";
    }

    protected override void ComputeRenderKeyCore(ref HashCode hc)
    {
        base.ComputeRenderKeyCore(ref hc);
        hc.Add(Dir);
        hc.Add(Direction);
    }
}
