@namespace Soenneker.Quark
@inherits Soenneker.Quark.Element
@implements IContainer

<div @attributes="BuildAttributes()">
	@ChildContent
</div>

@code {
	[Parameter]
	public string? ContainerType { get; set; }

    protected override void ApplyDefaultParameters()
    {
        base.ApplyDefaultParameters();
        DataSlot ??= "container";

        Margin ??= Quark.Margin.OnX.Auto;
        Width ??= Quark.Width.IsFull;
        MaxWidth ??= Quark.MaxWidth.Is7xl;
        Padding ??= Quark.Padding.OnX.Is4.OnMd.OnX.Is6.OnLg.OnX.Is8;
    }

    // Default to the common shadcn-style centered content wrapper.
    protected override void BuildAttributesCore(Dictionary<string, object> attributes)
    {
        base.BuildAttributesCore(attributes);

        if (ContainerType.HasContent())
        {
            BuildClassAttribute(attributes, (ref cls) =>
            {
                AppendClass(ref cls, ContainerType!);
            });
        }
    }

	protected override void ComputeRenderKeyCore(ref HashCode hc)
	{
		base.ComputeRenderKeyCore(ref hc);
		hc.Add(ContainerType);
	}
}