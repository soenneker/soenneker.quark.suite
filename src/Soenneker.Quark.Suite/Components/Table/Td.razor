@namespace Soenneker.Quark
@inherits Element

<td colspan="@Colspan" @attributes="BuildAttributes()">
    @ChildContent
</td>

@code {
    /// <summary>
    /// Gets or sets the colspan attribute value.
    /// </summary>
    [Parameter]
    public int Colspan { get; set; } = 1;

    protected override void ApplyDefaultParameters()
    {
        base.ApplyDefaultParameters();

        Padding ??= Quark.Padding.Is2;
        VerticalAlign ??= Quark.VerticalAlign.Middle;
        Whitespace ??= Quark.Whitespace.Nowrap;
    }

	protected override void BuildAttributesCore(Dictionary<string, object> attributes)
	{
        base.BuildAttributesCore(attributes);
		attributes["data-slot"] = "table-cell";

		BuildClassAttribute(attributes, (ref cls) =>
		{
			AppendClass(ref cls, "[&:has([role=checkbox])]:pr-0");
			AppendClass(ref cls, "[&>[role=checkbox]]:translate-y-[2px]");
		});
	}

    protected override void ComputeRenderKeyCore(ref HashCode hc)
    {
        base.ComputeRenderKeyCore(ref hc);
        hc.Add(Colspan);
    }
} 
