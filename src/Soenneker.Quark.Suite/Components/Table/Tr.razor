@namespace Soenneker.Quark
@inherits Element

<tr @key="@Key" @attributes="BuildAttributes()">
    @ChildContent
</tr>

@code {
    /// <summary>
    /// Gets or sets the key for the table row.
    /// </summary>
    [Parameter]
    public object? Key { get; set; }

    protected override void ApplyDefaultParameters()
    {
        base.ApplyDefaultParameters();
        Border ??= Quark.Border.Default.FromBottom;
        Transition ??= Quark.Transition.Colors;

    }

	protected override void BuildAttributesCore(Dictionary<string, object> attributes)
	{
		base.BuildAttributesCore(attributes);

		attributes["data-slot"] = "table-row";

		BuildClassAttribute(attributes, (ref cls) =>
		{
			AppendClass(ref cls, "hover:bg-muted/50 has-aria-expanded:bg-muted/50 data-[state=selected]:bg-muted");
		});
	}

    protected override void ComputeRenderKeyCore(ref HashCode hc)
    {
        base.ComputeRenderKeyCore(ref hc);
        hc.Add(Key);
    }
} 
