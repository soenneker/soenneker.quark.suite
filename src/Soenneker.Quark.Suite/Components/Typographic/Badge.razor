@namespace Soenneker.Quark
@inherits Soenneker.Quark.TypographyElement
@implements IBadge

@if (AsChild)
{
    <CascadingValue Name="@SlotAttributes.Name" Value="BuildAttributes()" IsFixed="false">
        @ChildContent
    </CascadingValue>
}
else
{
    <span @attributes="BuildAttributes()">@ChildContent</span>
}

@code {
    private const string _shadcnBadgeBase =
        "group/badge focus-visible:border-ring focus-visible:ring-[3px] focus-visible:ring-ring/50 has-data-[icon=inline-end]:pr-1.5 has-data-[icon=inline-start]:pl-1.5 aria-invalid:border-destructive aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 [&>svg]:pointer-events-none [&>svg]:size-3!";

    /// <summary>
    /// Gets or sets the visual style variant (shadcn/ui).
    /// </summary>
    [Parameter]
    public BadgeVariant Variant { get; set; } = BadgeVariant.Default;

    [Parameter]
    public bool AsChild { get; set; }

    protected override void BuildAttributesCore(Dictionary<string, object> attributes)
    {
        base.BuildAttributesCore(attributes);

        attributes["data-slot"] = "badge";
        attributes["data-variant"] = Variant?.Value ?? BadgeVariant.Default.Value;

        BuildClassAttribute(attributes, (ref cls) =>
        {
            AppendClass(ref cls, _shadcnBadgeBase);
            AppendClass(ref cls, GetBadgeVariantClass(Variant?.Value));
        });
    }

    protected override void ApplyDefaultParameters()
    {
        Display ??= Quark.Display.InlineFlex;
        Height ??= Quark.Height.Token("5");
        Width ??= Quark.Width.IsFit;
        Shrink ??= Quark.Shrink.Is0;
        ItemsAlign ??= Quark.Items.Center;
        Justify ??= Quark.Justify.Center;
        Gap ??= Quark.Gap.Is1;
        Overflow ??= Quark.Overflow.Hidden;
        Rounded ??= Quark.Rounded.Token("4xl");
        Border ??= Quark.Border.Default;
        BorderColor ??= Quark.BorderColor.Transparent;
        Padding ??= Quark.Padding.Is2.OnX.Token("0.5").OnY;
        Whitespace ??= Quark.Whitespace.Nowrap;
        Transition ??= Quark.Transition.All;
        TextSize ??= Quark.TextSize.Xs;
        FontWeight ??= Quark.FontWeight.Medium;

        ApplyVariantDefaults();

        base.ApplyDefaultParameters();
    }

    private void ApplyVariantDefaults()
    {
        switch (Variant?.Value)
        {
            case "secondary":
                BackgroundColor ??= Quark.BackgroundColor.Secondary;
                TextColor ??= Quark.TextColor.SecondaryForeground;
                break;
            case "destructive":
                BackgroundColor ??= Quark.BackgroundColor.Token("destructive/10");
                TextColor ??= Quark.TextColor.Destructive;
                break;
            case "outline":
                BorderColor ??= Quark.BorderColor.Border;
                TextColor ??= Quark.TextColor.Foreground;
                break;
            default:
                if (Variant?.Value != "ghost")
                {
                    BackgroundColor ??= Quark.BackgroundColor.Primary;
                    TextColor ??= Quark.TextColor.PrimaryForeground;
                }
                break;
        }
    }

    protected override void ComputeRenderKeyCore(ref HashCode hc)
    {
        base.ComputeRenderKeyCore(ref hc);
        hc.Add(Variant);
        hc.Add(AsChild);
    }

    private static string GetBadgeVariantClass(string? variant)
    {
        return variant switch
        {
            "secondary" => "[a]:hover:bg-secondary/80",
            "destructive" => "focus-visible:ring-destructive/20 dark:bg-destructive/20 dark:focus-visible:ring-destructive/40 [a]:hover:bg-destructive/20",
            "outline" => "[a]:hover:bg-muted [a]:hover:text-muted-foreground",
            "ghost" => "hover:bg-muted hover:text-muted-foreground dark:hover:bg-muted/50",
            _ => "[a]:hover:bg-primary/80"
        };
    }
}
