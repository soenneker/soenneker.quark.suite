@namespace Soenneker.Quark
@inherits Soenneker.Quark.Element
@using Soenneker.Bradix

<div @attributes="BuildAttributes()">@ChildContent</div>

@code {
    [Parameter]
    public Orientation Orientation { get; set; } = Soenneker.Bradix.Orientation.Horizontal;

    protected override void ApplyDefaultParameters()
    {
        Display ??= Quark.Display.Flex;
        Gap ??= Quark.Gap.Is2;

        base.ApplyDefaultParameters();
    }

    protected override void BuildAttributesCore(Dictionary<string, object> attributes)
    {
        base.BuildAttributesCore(attributes);
        attributes["data-slot"] = "suggestion-list";
        attributes["role"] = "group";
        attributes["aria-label"] = "Suggestions";

        BuildClassAttribute(attributes, (ref cls) =>
        {
            AppendClass(ref cls, "animate-in duration-150 fade-in-0");
            AppendClass(ref cls, Orientation == Soenneker.Bradix.Orientation.Vertical
                ? "flex-col items-start"
                : "flex-row flex-wrap items-center justify-center");
        });
    }
}
