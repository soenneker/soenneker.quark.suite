@using Soenneker.Bradix

@namespace Soenneker.Quark
@inherits Soenneker.Quark.Element

<BradixAvatar AdditionalAttributes="@AvatarAttributes">
    @ChildContent
</BradixAvatar>

@code {

    /// <summary>
    /// Size token aligned with shadcn avatar sizing.
    /// Supported values: default, sm, lg.
    /// </summary>
    [Parameter]
    public string Size { get; set; } = "default";

    private IReadOnlyDictionary<string, object> AvatarAttributes => BuildAttributes();

    protected override void ApplyDefaultParameters()
    {
        base.ApplyDefaultParameters();
        Position ??= Quark.Position.Relative;
        Display ??= Quark.Display.Flex;
        BoxSize ??= Quark.Size.Is8;
        Rounded ??= Quark.Rounded.Full;
        Shrink ??= Quark.Shrink.Is0;
        UserSelect ??= Quark.UserSelect.None;

    }

    protected override void BuildAttributesCore(Dictionary<string, object> attributes)
    {
        base.BuildAttributesCore(attributes);

        attributes["data-slot"] = "avatar";
        attributes["data-size"] = Size;

        BuildClassAttribute(attributes, (ref cls) =>
        {
            AppendClass(ref cls, "group/avatar after:absolute after:inset-0 after:rounded-full after:border after:border-border after:mix-blend-darken data-[size=lg]:size-10 data-[size=sm]:size-6 dark:after:mix-blend-lighten");
        });
    }

    protected override void ComputeRenderKeyCore(ref HashCode hc)
    {
        base.ComputeRenderKeyCore(ref hc);
        hc.Add(Size);
    }

}
