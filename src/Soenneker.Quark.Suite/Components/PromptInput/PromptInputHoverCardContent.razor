@namespace Soenneker.Quark
@inherits Soenneker.Quark.Element
@using Soenneker.Bradix

<HoverCardContent ForceMount="@ForceMount"
                  Side="@Side"
                  Align="@Align"
                  SideOffset="@SideOffset"
                  @attributes="BuildAttributes()">
    @ChildContent
</HoverCardContent>

@code {
    [Parameter] public bool ForceMount { get; set; }
    [Parameter] public Side Side { get; set; } = Soenneker.Bradix.Side.Top;
    [Parameter] public Alignment Align { get; set; } = Alignment.Start;
    [Parameter] public int SideOffset { get; set; } = 8;

    protected override void BuildAttributesCore(Dictionary<string, object> attributes)
    {
        base.BuildAttributesCore(attributes);
        attributes["data-slot"] = "prompt-input-hover-card-content";
    }

    protected override void ComputeRenderKeyCore(ref HashCode hc)
    {
        base.ComputeRenderKeyCore(ref hc);
        hc.Add(ForceMount);
        hc.Add(Side);
        hc.Add(Align);
        hc.Add(SideOffset);
    }
}
