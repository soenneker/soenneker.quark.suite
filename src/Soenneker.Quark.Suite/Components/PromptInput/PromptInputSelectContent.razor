@namespace Soenneker.Quark
@inherits Soenneker.Quark.Element
@using Soenneker.Bradix

<SelectContent ContentPosition="@ContentPosition"
               Side="@Side"
               SideOffset="@SideOffset"
               Align="@Align"
               @attributes="BuildAttributes()">
    @ChildContent
</SelectContent>

@code {
    [Parameter]
    public SelectPosition ContentPosition { get; set; } = SelectPosition.Popper;

    [Parameter]
    public Side Side { get; set; } = Soenneker.Bradix.Side.Top;

    [Parameter]
    public int SideOffset { get; set; } = 8;

    [Parameter]
    public Alignment Align { get; set; } = Alignment.Start;

    protected override void BuildAttributesCore(Dictionary<string, object> attributes)
    {
        base.BuildAttributesCore(attributes);
        attributes["data-slot"] = "prompt-input-select-content";
    }

    protected override void ComputeRenderKeyCore(ref HashCode hc)
    {
        base.ComputeRenderKeyCore(ref hc);
        hc.Add(ContentPosition);
        hc.Add(Side);
        hc.Add(SideOffset);
        hc.Add(Align);
    }
}
