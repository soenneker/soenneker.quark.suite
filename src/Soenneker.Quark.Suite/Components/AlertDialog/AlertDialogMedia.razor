@namespace Soenneker.Quark
@inherits Soenneker.Quark.Element
@implements IAlertDialogMedia

<div @attributes="BuildAttributes()">
    @ChildContent
</div>

@code {
    protected override void ApplyDefaultParameters()
    {
        base.ApplyDefaultParameters();

        Margin ??= Quark.Margin.Is2.FromBottom;
        Display ??= Quark.Display.InlineFlex;
        BoxSize ??= Quark.Size.Token("16");
        AlignUtility ??= Quark.Align.ItemsCenter.JustifyCenter;
        Rounded ??= Quark.Rounded.Md;
        BackgroundColor ??= Quark.BackgroundColor.Muted;
    }

    protected override void BuildAttributesCore(Dictionary<string, object> attributes)
    {
        base.BuildAttributesCore(attributes);
        attributes["data-slot"] = "alert-dialog-media";

        // shadcn AlertDialogMedia - new-york-v4 ui/alert-dialog.tsx
        BuildClassAttribute(attributes, (ref cls) =>
        {
            AppendClass(ref cls, "q-alert-dialog-media");
            AppendClass(ref cls, "sm:group-data-[size=default]/alert-dialog-content:row-span-2 *:[svg:not([class*='size-'])]:size-8");
        });
    }
}
