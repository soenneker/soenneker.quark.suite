@using Soenneker.Bradix

@namespace Soenneker.Quark
@inherits Soenneker.Quark.FormControlElement

<BradixMenubarItem Disabled="@Disabled"
                   OnSelect="HandleSelect"
                   AdditionalAttributes="@ItemAttributes">
    @ChildContent
</BradixMenubarItem>

@code {
    [Parameter]
    public bool Indented { get; set; }

    [Parameter]
    public bool Destructive { get; set; }

    private IReadOnlyDictionary<string, object> ItemAttributes => BuildItemAttributes();

    protected override void BuildAttributesCore(Dictionary<string, object> attributes)
    {
        base.BuildAttributesCore(attributes);

        attributes["data-slot"] = "menubar-item";
        if (Indented)
            attributes["data-inset"] = "true";
        attributes["data-variant"] = Destructive ? "destructive" : "default";
        BuildClassAttribute(attributes, (ref cls) =>
        {
            AppendClass(ref cls, "group/menubar-item focus:bg-accent focus:text-accent-foreground not-data-[variant=destructive]:focus:**:text-accent-foreground data-[disabled]:pointer-events-none data-[disabled]:opacity-50 data-inset:pl-7 data-[inset]:pl-7");
            AppendClass(ref cls, "data-[variant=destructive]:text-destructive data-[variant=destructive]:focus:bg-destructive/10 dark:data-[variant=destructive]:focus:bg-destructive/20 data-[variant=destructive]:focus:text-destructive data-[variant=destructive]:*:[svg]:text-destructive!");
            AppendClass(ref cls, "[&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4");
        });
    }

    protected override void ApplyDefaultParameters()
    {
        base.ApplyDefaultParameters();
        Display ??= Quark.Display.Flex;
        Position ??= Quark.Position.Relative;
        ItemsAlign ??= Items.Center;
        Gap ??= Quark.Gap.Token("1.5");
        Rounded ??= Quark.Rounded.Md;
        Padding ??= Quark.Padding.Token("1.5").OnX.Is1.OnY;
        TextSize ??= Quark.TextSize.Sm;
        OutlineStyle ??= "outline-hidden";
        UserSelect ??= Quark.UserSelect.None;
        Cursor ??= Quark.Cursor.Default;

    }

    private IReadOnlyDictionary<string, object> BuildItemAttributes()
    {
        return BuildAttributes();
    }

    protected override void ComputeRenderKeyCore(ref HashCode hc)
    {
        base.ComputeRenderKeyCore(ref hc);
        hc.Add(Indented);
        hc.Add(Disabled);
        hc.Add(Destructive);
    }

    private Task HandleSelect()
    {
        return Task.CompletedTask;
    }

    public override void Dispose() => base.Dispose();
}
