@using Soenneker.Bradix

@namespace Soenneker.Quark
@inherits Soenneker.Quark.FormControlElement
@implements INavigationMenuTrigger

<BradixNavigationMenuTrigger Disabled="@ResolvedDisabled"
                            AdditionalAttributes="@TriggerAttributes">
    @ChildContent
    @if (ShowChevron)
    {
        <Icon Name="LucideIcon.ChevronDown"
              Position="Quark.Position.Relative"
              Margin="Quark.Margin.Is1.FromLeft"
              Size="Quark.Size.Is3"
              Class="top-[1px] transition duration-300 group-data-[state=open]:rotate-180"
              aria-hidden="true" />
    }
</BradixNavigationMenuTrigger>

@code {
    [Parameter]
    public bool Open { get; set; }

    [Parameter]
    public bool ShowChevron { get; set; } = true;

    [CascadingParameter]
    private NavigationMenuItem? ParentNavigationMenuItem { get; set; }

    private bool ResolvedDisabled => Disabled || ParentNavigationMenuItem?.Disabled == true;
    private IReadOnlyDictionary<string, object> TriggerAttributes => BuildAttributes();

    protected override void ApplyDefaultParameters()
    {
        base.ApplyDefaultParameters();

        Display ??= Quark.Display.InlineFlex;
        ItemsAlign ??= Items.Center;
        Justify ??= Quark.Justify.Center;
        TextSize ??= Quark.TextSize.Sm;
        FontWeight ??= Quark.FontWeight.Medium;
        Height ??= Quark.Height.Is9;
        Width ??= Quark.Width.IsMax;
        BackgroundColor ??= Quark.BackgroundColor.Background;
        Rounded ??= Quark.Rounded.Md;
        Padding ??= Quark.Padding.Is4.OnX.Is2.OnY;
        OutlineStyle ??= Quark.OutlineStyle.None;
    }

    protected override void BuildAttributesCore(Dictionary<string, object> attributes)
    {
        base.BuildAttributesCore(attributes);

        attributes["data-slot"] = "navigation-menu-trigger";

        BuildClassAttribute(attributes, (ref cls) =>
        {
            AppendClass(ref cls, "group/navigation-menu-trigger transition-[color,box-shadow] hover:bg-accent hover:text-accent-foreground focus:bg-accent focus:text-accent-foreground");
            AppendClass(ref cls, "focus-visible:ring-3 focus-visible:ring-ring/50 focus-visible:outline-1");
            AppendClass(ref cls, "disabled:pointer-events-none disabled:opacity-50");
            AppendClass(ref cls, "data-[state=open]:bg-accent/50 data-[state=open]:text-accent-foreground data-[state=open]:hover:bg-accent data-[state=open]:focus:bg-accent data-open:bg-accent/50 data-open:text-accent-foreground data-open:hover:bg-accent data-open:focus:bg-accent");
            AppendClass(ref cls, "group");
        });
    }

    protected override void ComputeRenderKeyCore(ref HashCode hc)
    {
        base.ComputeRenderKeyCore(ref hc);
        hc.Add(Open);
        hc.Add(Disabled);
        hc.Add(ParentNavigationMenuItem?.Disabled);
        hc.Add(ShowChevron);
    }
}
