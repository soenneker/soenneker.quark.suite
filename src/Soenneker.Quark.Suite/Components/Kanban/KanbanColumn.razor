@namespace Soenneker.Quark
@inherits Soenneker.Quark.Element

<div @attributes="BuildAttributes()">
    @ChildContent
</div>

@code {
    private const string _baseClass =
        "group/kanban-column flex max-h-full min-h-40 w-72 shrink-0 flex-col rounded-xl border bg-muted/40 p-2 shadow-xs transition-colors data-[disabled=true]:opacity-60";

    [Parameter]
    public string? Value { get; set; }

    [Parameter]
    public bool Disabled { get; set; }

    protected override void ApplyDefaultParameters()
    {
        base.ApplyDefaultParameters();
        DataSlot ??= "kanban-column";
        Role ??= "listitem";
    }

    protected override void BuildAttributesCore(Dictionary<string, object> attributes)
    {
        base.BuildAttributesCore(attributes);

        attributes["data-sortable-item"] = string.Empty;
        attributes["data-kanban-column"] = string.Empty;

        if (Value.HasContent())
            attributes["data-sortable-id"] = Value!;

        if (Disabled)
        {
            attributes["data-disabled"] = "true";
            attributes["aria-disabled"] = "true";
        }

        BuildClassAttribute(attributes, (ref cls) =>
        {
            AppendClass(ref cls, _baseClass);
        });
    }

    protected override void ComputeRenderKeyCore(ref HashCode hc)
    {
        base.ComputeRenderKeyCore(ref hc);
        hc.Add(Value);
        hc.Add(Disabled);
    }
}
