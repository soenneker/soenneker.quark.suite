@namespace Soenneker.Quark
@inherits Soenneker.Quark.Element
@implements ILegend

<legend @attributes="BuildAttributes()">
	@ChildContent
</legend>

@code {
    protected override void ApplyDefaultParameters()
    {
        base.ApplyDefaultParameters();

        Margin ??= Quark.Margin.Is3.FromBottom;
        FontWeight ??= Quark.FontWeight.Medium;
    }

    protected override void BuildAttributesCore(Dictionary<string, object> attributes)
    {
        base.BuildAttributesCore(attributes);

        attributes["data-slot"] = "field-legend";
        attributes["data-variant"] = "legend";

        BuildClassAttribute(attributes, (ref cls) =>
        {
            AppendClass(ref cls, "q-legend");
            AppendClass(ref cls, "data-[variant=legend]:text-base");
            AppendClass(ref cls, "data-[variant=label]:text-sm");
        });
    }
}
