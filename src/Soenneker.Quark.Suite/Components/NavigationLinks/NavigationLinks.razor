@namespace Soenneker.Quark

@if (Mode == NavigationLinksMode.Header)
{
    @foreach (var item in Items)
    {
        @if (item.Href is not null)
        {
            <Anchor To="@item.Href" class="@GetHeaderItemClass(item)">
                @item.Title
                @if (item.IsNew)
                {
                    <Span BoxSize="Size.Is2" class="rounded-full bg-blue-500"></Span>
                }
            </Anchor>
        }
        else
        {
            <Span class="@($"{GetHeaderItemClass(item)} opacity-75")">
                @item.Title
            </Span>
        }
    }
}
else if (Mode == NavigationLinksMode.Stack)
{
    <Div class="flex flex-col gap-4">
        @if (!string.IsNullOrWhiteSpace(Title))
        {
            <Div class="text-sm font-medium text-muted-foreground">@Title</Div>
        }

        <Div class="flex flex-col gap-3">
            @foreach (var item in Items)
            {
                @if (item.Href is not null)
                {
                    <Anchor To="@item.Href" class="@GetStackItemClass(item)">
                        @item.Title
                        @if (item.IsNew)
                        {
                            <Span BoxSize="Size.Is2" class="flex rounded-full bg-blue-500" title="New"></Span>
                        }
                    </Anchor>
                }
                else
                {
                    <Span class="@($"{GetStackItemClass(item)} opacity-70")">
                        @item.Title
                    </Span>
                }
            }
        </Div>
    </Div>
}
else
{
    <SidebarGroup Class="@GroupClass">
        @if (!string.IsNullOrWhiteSpace(Title))
        {
            <SidebarGroupLabel>@Title</SidebarGroupLabel>
        }

        <SidebarGroupContent>
            <SidebarMenu>
                @foreach (var item in Items)
                {
                    <SidebarMenuItem>
                        @if (item.Href is not null)
                        {
                            <SidebarMenuButton To="@item.Href"
                                               Active="@IsItemActive(item)"
                                               Variant="SidebarMenuButtonVariant.Navigation">
                                <Span>@item.Title</Span>
                                @if (item.IsNew)
                                {
                                    <Span BoxSize="Size.Is2" class="flex rounded-full bg-blue-500" title="New"></Span>
                                }
                            </SidebarMenuButton>
                        }
                        else
                        {
                            <SidebarMenuButton Variant="SidebarMenuButtonVariant.Navigation"
                                               Class="opacity-70"
                                               aria-disabled="true"
                                               tabindex="-1">
                                <Span>@item.Title</Span>
                                @if (item.IsNew)
                                {
                                    <Span BoxSize="Size.Is2" class="flex rounded-full bg-blue-500" title="New"></Span>
                                }
                            </SidebarMenuButton>
                        }
                    </SidebarMenuItem>
                }
            </SidebarMenu>
        </SidebarGroupContent>
    </SidebarGroup>
}

@code {
    [Parameter]
    public IEnumerable<NavigationItem> Items { get; set; } = [];

    [Parameter]
    public NavigationLinksMode Mode { get; set; } = NavigationLinksMode.Sidebar;

    [Parameter]
    public string? Title { get; set; }

    [Parameter]
    public string? GroupClass { get; set; }

    [Parameter]
    public Func<NavigationItem, bool>? IsActive { get; set; }

    private bool IsItemActive(NavigationItem item)
    {
        return IsActive?.Invoke(item) ?? false;
    }

    private string GetHeaderItemClass(NavigationItem item)
    {
        const string baseClass = "inline-flex shrink-0 items-center justify-center gap-1.5 rounded-md border border-transparent bg-clip-padding px-2.5 text-sm font-medium whitespace-nowrap transition-all outline-none select-none focus-visible:border-ring focus-visible:ring-[3px] focus-visible:ring-ring/50 disabled:pointer-events-none disabled:opacity-50 aria-invalid:border-destructive aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 [&_svg]:pointer-events-none [&_svg]:shrink-0 [&_svg:not([class*='size-'])]:size-4 h-8 has-[>svg]:px-2.5 relative";

        return IsItemActive(item)
            ? $"{baseClass} text-foreground bg-accent dark:bg-accent/50"
            : $"{baseClass} text-foreground/70 hover:bg-accent hover:text-accent-foreground dark:hover:bg-accent/50";
    }

    private string GetStackItemClass(NavigationItem item)
    {
        const string baseClass = "inline-flex items-center gap-2 text-2xl leading-none font-medium transition-all";

        return IsItemActive(item)
            ? $"{baseClass} text-foreground"
            : $"{baseClass} text-foreground hover:text-foreground/80";
    }
}
