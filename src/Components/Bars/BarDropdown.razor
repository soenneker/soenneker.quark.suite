@inherits Soenneker.Quark.Element

<Div Class="dropdown" @attributes="BuildAttributes()" >
    @ChildContent
</Div>

@code {
	public override string? ThemeKey { get; set; } = "BarDropdown";

    [Parameter]
    public bool Visible { get; set; }

    [Parameter]
    public bool RightAligned { get; set; }

    protected override Dictionary<string, object> BuildAttributes()
    {
        Dictionary<string, object> attributes = base.BuildAttributes();

        var baseClass = "dropdown";
        
        if (Visible)
            baseClass = $"{baseClass} show";

        if (RightAligned)
            baseClass = $"{baseClass} dropdown-menu-end";

        AppendToClassAttr(attributes, baseClass);

        return attributes;
    }

}
