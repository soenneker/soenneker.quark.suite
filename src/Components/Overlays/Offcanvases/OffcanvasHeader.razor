@namespace Soenneker.Quark
@inherits Soenneker.Quark.Element
@implements IOffcanvasHeader

<div @attributes="BuildAttributes()">
    @if (Title is not null)
    {
        <h5 class="q-offcanvas-title">@Title</h5>
    }
    @ChildContent
    @if (ShowCloseButton)
    {
        <Button Type="@ButtonType.Button" Class="rounded-sm opacity-70 ring-offset-background transition-opacity hover:opacity-100 focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2" OnClick="HandleClose"></Button>
    }
    
</div>

@code {
    // shadcn - new-york-v4
    /// <summary>
    /// Gets or sets whether to show the close button in the header.
    /// </summary>
    [Parameter]
    public bool ShowCloseButton { get; set; } = true;

    [CascadingParameter(Name = nameof(ParentOffcanvas))]
    public Offcanvas? ParentOffcanvas { get; set; }

    protected override void BuildAttributesCore(Dictionary<string, object> attrs)
    {
        BuildClassAttribute(attrs, (ref cls) =>
        {
            AppendClass(ref cls, "q-offcanvas-header");
            AppendClass(ref cls, "flex items-center justify-between p-4 border-b");
        });
    }

    private async Task HandleClose(MouseEventArgs args)
    {
        if (ParentOffcanvas is not null)
            await ParentOffcanvas.RequestClose();
    }

    protected override void ComputeRenderKeyCore(ref HashCode hc)
    {
        hc.Add(ShowCloseButton);
    }
}