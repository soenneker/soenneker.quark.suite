@namespace Soenneker.Quark
@inherits Soenneker.Quark.Element
@implements IAlertDialogDescription

<Paragraph @attributes="BuildAttributes()">
    @ChildContent
</Paragraph>

@code {
    protected override void BuildAttributesCore(Dictionary<string, object> attributes)
    {
        base.BuildAttributesCore(attributes);

        // shadcn AlertDialogDescription - new-york-v4 ui/alert-dialog.tsx
        BuildClassAttribute(attributes, (ref cls) =>
        {
            AppendClass(ref cls, "q-alert-dialog-description");
            AppendClass(ref cls, "text-muted-foreground text-sm");
        });
    }
}
