@namespace Soenneker.Quark
@inherits Soenneker.Quark.Element
@implements ICardTitle

<Heading Scale="@Scale.Is5" @attributes="BuildAttributes()">
    @ChildContent
</Heading>

@code {
    // shadcn CardTitle - new-york-v4 ui/card.tsx
    protected override void BuildAttributesCore(Dictionary<string, object> attributes)
    {
        attributes.TryAdd("data-slot", "card-title");

        BuildClassAttribute(attributes, (ref cls) =>
        {
            AppendClass(ref cls, "q-card-title");
            AppendClass(ref cls, "leading-none font-semibold");
        });
    }
}