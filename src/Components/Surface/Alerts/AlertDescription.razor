@namespace Soenneker.Quark
@inherits Soenneker.Quark.Element
@implements IAlertDescription

<div @attributes="BuildAttributes()">
    @ChildContent
</div>

@code {
    [CascadingParameter(Name = nameof(ParentAlert))]
    protected Alert? ParentAlert { get; set; }

    protected override void OnInitialized()
    {
        ParentAlert?.NotifyHasDescription();
        base.OnInitialized();
    }

    protected override void BuildAttributesCore(Dictionary<string, object> attributes)
    {
        attributes["data-slot"] = "alert-description";
        BuildClassAttribute(attributes, (ref cls) =>
        {
            AppendClass(ref cls, "q-alert-description");
            AppendClass(ref cls, "text-muted-foreground text-sm [&_p]:leading-relaxed");
        });
    }
}
