@namespace Soenneker.Quark
@inherits Soenneker.Quark.TypographicElement

<p @attributes="BuildTypographicAttributes()">@ChildContent</p>

@code {
    public override string? ThemeKey { get; set; } = "Lead";

    protected override Dictionary<string, object> BuildTypographicAttributes()
    {
        var attributes = base.BuildTypographicAttributes();
        BuildClassAttribute(attributes, (ref PooledStringBuilder cls) => AppendClass(ref cls, "lead"));
        return attributes;
    }
}

