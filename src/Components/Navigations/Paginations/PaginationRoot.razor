@namespace Soenneker.Quark
@inherits Soenneker.Quark.Element

@* shadcn - new-york-v4 ui/pagination.tsx Pagination (nav) *@
<nav role="navigation" aria-label="pagination" @attributes="BuildAttributes()">
    @ChildContent
</nav>

@code {
    protected override void BuildAttributesCore(Dictionary<string, object> attributes)
    {
        base.BuildAttributesCore(attributes);

        attributes["data-slot"] = "pagination";

        BuildClassAttribute(attributes, (ref cls) =>
        {
            AppendClass(ref cls, "q-pagination-root");
            AppendClass(ref cls, "mx-auto flex w-full justify-center");
        });
    }
}
