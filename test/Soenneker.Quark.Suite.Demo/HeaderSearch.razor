@inject Microsoft.Extensions.Configuration.IConfiguration Configuration

@if (_searchConfiguration is not null)
{
    <Div Class="w-32 shrink-0 focus-within:fixed focus-within:inset-x-4 focus-within:top-4 focus-within:z-60 focus-within:w-auto sm:w-52 sm:focus-within:static sm:focus-within:z-auto sm:focus-within:w-52">
        <CloudflareAiSearch Configuration="_searchConfiguration" />
    </Div>
}

@code {
    private CloudflareAiSearchBarConfiguration? _searchConfiguration;

    protected override void OnInitialized()
    {
        string? apiUrl = Configuration["CloudflareAiSearch:ApiUrl"];

        if (string.IsNullOrWhiteSpace(apiUrl))
            return;

        _searchConfiguration = new CloudflareAiSearchBarConfiguration
        {
            ApiUrl = apiUrl,
            Placeholder = "Search",
            MaxResults = 50,
            MaxRenderResults = 10,
            DebounceMilliseconds = 250,
            ShowUrl = true
        };
    }
}
