@page "/containers"
@using Microsoft.Extensions.Logging

@inject ILogger<Index> Logger

<Container>
    <img src="https://user-images.githubusercontent.com/4441470/224455560-91ed3ee7-f510-4041-a8d2-3fc093025112.png" />
    <h1>Containers</h1>
    <p>This page demonstrates the Bootstrap Container component from Quark.Containers.</p>
    <br />

    <hr />

    <h2>Basic Container Usage</h2>
    <p>The Container component provides a responsive Bootstrap container that centers content and provides appropriate padding.</p>
    
    <Container Class="bg-light p-3 mb-3">
        <h3>Nested Container Example</h3>
        <p>This is content inside a nested container with Bootstrap utility classes.</p>
        <p>The container automatically handles responsive behavior and centering.</p>
    </Container>

    <h2>Container with Custom Styling</h2>
    <Container Style="border: 2px solid #007bff; border-radius: 8px; background-color: #f8f9fa;">
        <h3>Styled Container</h3>
        <p>This container demonstrates custom styling while maintaining Bootstrap container behavior.</p>
        <ul>
            <li>Responsive width management</li>
            <li>Automatic centering</li>
            <li>Consistent padding</li>
            <li>Custom styling support</li>
        </ul>
    </Container>

    <h2>Container Properties</h2>
    <Container Class="bg-info text-white p-3">
        <h3>Container Features</h3>
        <p>The Container component inherits all properties from the Element base class:</p>
        <ul>
            <li><strong>Id:</strong> HTML id attribute</li>
            <li><strong>Class:</strong> CSS classes (Bootstrap utility classes supported)</li>
            <li><strong>Style:</strong> Inline CSS styles</li>
            <li><strong>Title:</strong> Tooltip text</li>
            <li><strong>Hidden:</strong> Visibility control</li>
            <li><strong>Display:</strong> CSS display property</li>
            <li><strong>Margin/Padding:</strong> Spacing utilities</li>
            <li><strong>Event Handlers:</strong> Click, focus, keyboard events</li>
        </ul>
    </Container>

    <h2>Class Property Behavior</h2>
    <p>The Container component automatically ensures the Bootstrap "container" class is always present, even when you specify additional classes:</p>
    
    <Container Class="bg-success text-white p-3 mb-3">
        <h3>Custom Classes with Container</h3>
        <p>This container has custom Bootstrap classes (bg-success, text-white, p-3, mb-3) but the "container" class is automatically included.</p>
        <p><strong>Result:</strong> class="container bg-success text-white p-3 mb-3"</p>
    </Container>

    <Container Class="border border-primary rounded">
        <h3>Another Example</h3>
        <p>This container demonstrates that custom classes are properly combined with the container class.</p>
        <p><strong>Result:</strong> class="container border border-primary rounded"</p>
    </Container>
</Container>

@code{
    protected override void OnInitialized()
    {
        Logger.LogInformation("Container demo page initialized");
    }
}
