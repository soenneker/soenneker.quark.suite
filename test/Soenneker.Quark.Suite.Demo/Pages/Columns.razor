@page "/columns"
@using Microsoft.Extensions.Logging

@inject ILogger<Index> Logger

<div class="container">
    <img src="https://user-images.githubusercontent.com/4441470/224455560-91ed3ee7-f510-4041-a8d2-3fc093025112.png" />
    <h1>Soenneker.Quark.Columns Demo</h1>
    <p>This page demonstrates the Bootstrap Column component from Quark.Columns.</p>
    <br />

    <hr />

    <h2>Basic Column Usage</h2>
    <p>The Column component provides a Bootstrap column that automatically sizes and provides proper spacing.</p>
    
    <div class="row mb-3">
        <Column Class="bg-primary text-white p-3">
            <h3>Column with Custom Classes</h3>
            <p>This column has custom Bootstrap classes (bg-primary, text-white, p-3) but the "col" class is automatically included.</p>
            <p><strong>Result:</strong> class="col bg-primary text-white p-3"</p>
        </Column>
    </div>

    <h2>Responsive Columns</h2>
    <div class="row mb-3">
        <Column Class="col-md-4 bg-success text-white p-3 text-center">
            <h4>Small Column</h4>
            <p>This column uses col-md-4 for responsive sizing.</p>
        </Column>
        <Column Class="col-md-4 bg-warning text-dark p-3 text-center">
            <h4>Medium Column</h4>
            <p>This column also uses col-md-4 for equal width.</p>
        </Column>
        <Column Class="col-md-4 bg-info text-white p-3 text-center">
            <h4>Large Column</h4>
            <p>This column completes the row with col-md-4.</p>
        </Column>
    </div>

    <h2>Column Sizing Examples</h2>
    <div class="row mb-3">
        <Column Class="col-6 bg-danger text-white p-3 text-center">
            <h4>Half Width</h4>
            <p>This column takes up half the row width (col-6).</p>
        </Column>
        <Column Class="col-6 bg-dark text-white p-3 text-center">
            <h4>Half Width</h4>
            <p>This column also takes up half the row width (col-6).</p>
        </Column>
    </div>

    <div class="row mb-3">
        <Column Class="col-4 bg-secondary text-white p-3 text-center">
            <h4>One Third</h4>
            <p>This column takes up one third of the row (col-4).</p>
        </Column>
        <Column Class="col-8 bg-light text-dark p-3 text-center">
            <h4>Two Thirds</h4>
            <p>This column takes up two thirds of the row (col-8).</p>
        </Column>
    </div>

    <h2>Column CSS Values</h2>
    <p>Now you can use CSS values for column sizing, offsetting, and ordering:</p>
    
    <div class="row mb-3">
        <Column ColumnSize="@ColumnSize.Is6" Class="bg-primary text-white p-3 text-center">
            <h4>CSS Value Column Size</h4>
            <p>This column uses ColumnSize.Is6 (equivalent to col-6).</p>
        </Column>
        <Column ColumnSize="@ColumnSize.Is6" Class="bg-success text-white p-3 text-center">
            <h4>CSS Value Column Size</h4>
            <p>This column also uses ColumnSize.Is6.</p>
        </Column>
    </div>

    <div class="row mb-3">
        <Column ColumnSize="@ColumnSize.Is4" ColumnOffset="ColumnOffset.O1" Class="bg-warning text-dark p-3 text-center">
            <h4>Column with Offset</h4>
            <p>This column uses ColumnSize.Is4 with ColumnOffset.Is2.</p>
        </Column>
        <Column ColumnSize="@ColumnSize.Is4" Class="bg-info text-white p-3 text-center">
            <h4>Regular Column</h4>
            <p>This column uses ColumnSize.Is4.</p>
        </Column>
    </div>

    <div class="row mb-3">
        <Column ColumnSize="@ColumnSize.Is4" ColumnOrder="ColumnOrder.O3" Class="bg-danger text-white p-3 text-center">
            <h4>Order 3</h4>
            <p>This column appears third due to ColumnOrder.Is3.</p>
        </Column>
        <Column ColumnSize="@ColumnSize.Is4" ColumnOrder="ColumnOrder.O1" Class="bg-secondary text-white p-3 text-center">
            <h4>Order 1</h4>
            <p>This column appears first due to ColumnOrder.Is1.</p>
        </Column>
        <Column ColumnSize="@ColumnSize.Is4" ColumnOrder="ColumnOrder.O2" Class="bg-dark text-white p-3 text-center">
            <h4>Order 2</h4>
            <p>This column appears second due to ColumnOrder.Is2.</p>
        </Column>
    </div>

    <h2>Column Properties</h2>
    <div class="row">
        <Column Class="col-12 bg-info text-white p-3">
            <h3>Column Features</h3>
            <p>The Column component inherits all properties from the Element base class:</p>
            <ul>
                <li><strong>Id:</strong> HTML id attribute</li>
                <li><strong>Class:</strong> CSS classes (Bootstrap utility classes supported)</li>
                <li><strong>Style:</strong> Inline CSS styles</li>
                <li><strong>Title:</strong> Tooltip text</li>
                <li><strong>Hidden:</strong> Visibility control</li>
                <li><strong>Display:</strong> CSS display property</li>
                <li><strong>Margin/Padding:</strong> Spacing utilities</li>
                <li><strong>Event Handlers:</strong> Click, focus, keyboard events</li>
                <li><strong>ColumnSize:</strong> CSS value for column sizing (Is1-Is12, Auto)</li>
                <li><strong>ColumnOffset:</strong> CSS value for column offsetting</li>
                <li><strong>ColumnOrder:</strong> CSS value for column ordering</li>
            </ul>
        </Column>
    </div>

    <h2>Auto-sizing Columns</h2>
    <div class="row mb-3">
        <Column Class="bg-primary text-white p-3 text-center">
            <h4>Auto Column 1</h4>
            <p>This column auto-sizes based on content.</p>
        </Column>
        <Column Class="bg-success text-white p-3 text-center">
            <h4>Auto Column 2</h4>
            <p>This column also auto-sizes.</p>
        </Column>
        <Column Class="bg-warning text-dark p-3 text-center">
            <h4>Auto Column 3</h4>
            <p>All columns share the remaining space equally.</p>
        </Column>
    </div>
</div>

@code{
    protected override void OnInitialized()
    {
        Logger.LogInformation("Column demo page initialized");
    }
}
