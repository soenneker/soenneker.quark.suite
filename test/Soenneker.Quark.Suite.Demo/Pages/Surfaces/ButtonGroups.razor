@page "/surfaces/button-groups"

<PageTitle>Button Group - Quark Suite</PageTitle>

<Div Class="space-y-8 max-w-4xl">
    <Div>
        <Div Class="space-y-3">
            <H1 Class="text-4xl font-bold tracking-tight">Button Group</H1>
            <Paragraph Class="text-xl text-muted-foreground" Margin="Margin.Is0">
                Group buttons on a single line or stack them vertically. Use with Dropdown for split actions.
            </Paragraph>
        </Div>
    </Div>

    <Div Class="space-y-6">
        <section class="space-y-4">
            <DemoSection Title="Basic" Description="Buttons in a group share borders and sit on one line."
                        Code="@(@"<ButtonGroup>
    <Button BackgroundColor=""BackgroundColor.Primary"">Left</Button>
    <Button BackgroundColor=""BackgroundColor.Primary"">Middle</Button>
    <Button BackgroundColor=""BackgroundColor.Primary"">Right</Button>
</ButtonGroup>")">
                <ButtonGroup>
                    <Button BackgroundColor="BackgroundColor.Primary">Left</Button>
                    <Button BackgroundColor="BackgroundColor.Primary">Middle</Button>
                    <Button BackgroundColor="BackgroundColor.Primary">Right</Button>
                </ButtonGroup>
            </DemoSection>
        </section>

        <section class="space-y-4">
            <DemoSection Title="Mixed styles" Description="Each button can have a different BackgroundColor.">
                <ButtonGroup>
                    <Button BackgroundColor="BackgroundColor.Primary">Primary</Button>
                    <Button BackgroundColor="BackgroundColor.Secondary">Secondary</Button>
                    <Button BackgroundColor="BackgroundColor.Success">Success</Button>
                    <Button BackgroundColor="BackgroundColor.Warning">Warning</Button>
                    <Button BackgroundColor="BackgroundColor.Danger">Danger</Button>
                </ButtonGroup>
            </DemoSection>
            <DemoSection Title="Outline" Description="Use Outline=true on buttons for bordered style.">
                <ButtonGroup>
                    <Button BackgroundColor="BackgroundColor.Primary" Outline="true">Left</Button>
                    <Button BackgroundColor="BackgroundColor.Primary" Outline="true">Middle</Button>
                    <Button BackgroundColor="BackgroundColor.Primary" Outline="true">Right</Button>
                </ButtonGroup>
            </DemoSection>
        </section>

        <section class="space-y-4">
            <DemoSection Title="Vertical" Description="Set Vertical=true to stack buttons.">
                <ButtonGroup Vertical="true">
                    <Button BackgroundColor="BackgroundColor.Primary">Button</Button>
                    <Button BackgroundColor="BackgroundColor.Primary">Button</Button>
                    <Button BackgroundColor="BackgroundColor.Primary">Button</Button>
                    <Button BackgroundColor="BackgroundColor.Primary">Button</Button>
                </ButtonGroup>
            </DemoSection>
        </section>

        <section class="space-y-4">
            <DemoSection Title="Sizes" Description="Use Size to control the group and its buttons (Sm, Default, Lg).">
                <Div Class="flex flex-wrap gap-6">
                    <Div>
                        <H3 Class="mb-2 text-sm font-medium text-muted-foreground">Small</H3>
                        <ButtonGroup Size="ButtonSize.Sm">
                            <Button BackgroundColor="BackgroundColor.Primary">Left</Button>
                            <Button BackgroundColor="BackgroundColor.Primary">Middle</Button>
                            <Button BackgroundColor="BackgroundColor.Primary">Right</Button>
                        </ButtonGroup>
                    </Div>
                    <Div>
                        <H3 Class="mb-2 text-sm font-medium text-muted-foreground">Default</H3>
                        <ButtonGroup>
                            <Button BackgroundColor="BackgroundColor.Primary">Left</Button>
                            <Button BackgroundColor="BackgroundColor.Primary">Middle</Button>
                            <Button BackgroundColor="BackgroundColor.Primary">Right</Button>
                        </ButtonGroup>
                    </Div>
                    <Div>
                        <H3 Class="mb-2 text-sm font-medium text-muted-foreground">Large</H3>
                        <ButtonGroup Size="ButtonSize.Lg">
                            <Button BackgroundColor="BackgroundColor.Primary">Left</Button>
                            <Button BackgroundColor="BackgroundColor.Primary">Middle</Button>
                            <Button BackgroundColor="BackgroundColor.Primary">Right</Button>
                        </ButtonGroup>
                    </Div>
                </Div>
            </DemoSection>
        </section>

        <section class="space-y-4">
            <DemoSection Title="With icons" Description="Add Icon components inside buttons for navigation or actions.">
                <ButtonGroup>
                    <Button BackgroundColor="BackgroundColor.Primary"><Icon Name="chevron-left"/> Previous</Button>
                    <Button BackgroundColor="BackgroundColor.Primary"><Icon Name="home"/> Home</Button>
                    <Button BackgroundColor="BackgroundColor.Primary">Next <Icon Name="chevron-right"/></Button>
                </ButtonGroup>
            </DemoSection>
        </section>

        <section class="space-y-4">
            <DemoSection Title="With dropdown" Description="Combine a ButtonGroup with a Dropdown for split actions or menus.">
                <ButtonGroup>
                    <Button BackgroundColor="BackgroundColor.Primary">Action</Button>
                    <Button BackgroundColor="BackgroundColor.Primary">Another action</Button>
                    <Dropdown>
                        <DropdownToggle BackgroundColor="BackgroundColor.Primary">Dropdown <Icon Name="chevron-down"/></DropdownToggle>
                        <DropdownMenu>
                            <DropdownItem>Action</DropdownItem>
                            <DropdownItem>Another action</DropdownItem>
                            <DropdownDivider/>
                            <DropdownItem>Something else</DropdownItem>
                        </DropdownMenu>
                    </Dropdown>
                </ButtonGroup>
            </DemoSection>
        </section>

        <section class="space-y-4">
            <DemoSection Title="Toolbar" Description="Use multiple groups with role=\"toolbar\" for accessibility.">
                <Div Class="flex flex-wrap gap-2" role="toolbar" aria-label="Toolbar">
                    <ButtonGroup role="group" aria-label="First group">
                        <Button BackgroundColor="BackgroundColor.Primary">1</Button>
                        <Button BackgroundColor="BackgroundColor.Primary">2</Button>
                        <Button BackgroundColor="BackgroundColor.Primary">3</Button>
                        <Button BackgroundColor="BackgroundColor.Primary">4</Button>
                    </ButtonGroup>
                    <ButtonGroup role="group" aria-label="Second group">
                        <Button BackgroundColor="BackgroundColor.Secondary">5</Button>
                        <Button BackgroundColor="BackgroundColor.Secondary">6</Button>
                        <Button BackgroundColor="BackgroundColor.Secondary">7</Button>
                    </ButtonGroup>
                    <ButtonGroup role="group" aria-label="Third group">
                        <Button BackgroundColor="BackgroundColor.Info">8</Button>
                    </ButtonGroup>
                </Div>
            </DemoSection>
        </section>
    </Div>
</Div>
