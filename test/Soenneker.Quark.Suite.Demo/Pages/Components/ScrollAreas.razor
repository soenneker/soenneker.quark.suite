@page "/scroll-area"

<PageTitle>Scroll Area - Quark Suite</PageTitle>

<Div Space="Space.Y.Is8" MaxWidth="MaxWidth.Is4xl">
    <Div Space="Space.Y.Is6">
        <Section Space="Space.Y.Is4">
            <DemoSection Title="Demo"
                         Description="Fixed-height tag list with separators between items."
                         Code="@VerticalCode"
                         CodeLanguage="razor">
                <ScrollArea Class="h-72 w-48 rounded-md border">
                    <Div Class="p-4">
                        <H4 TextSize="TextSize.Sm" FontWeight="FontWeight.Medium" Class="mb-4 leading-none">Tags</H4>
                        @for (var i = 0; i < _tags.Length; i++)
                        {
                            var index = i;

                            <Div class="text-sm">@_tags[index]</Div>

                            @if (index < _tags.Length - 1)
                            {
                                <Separator Class="my-2" />
                            }
                        }
                    </Div>
                </ScrollArea>
            </DemoSection>
        </Section>

        <Section Space="Space.Y.Is4">
            <DemoSection Title="Horizontal"
                         Description="Horizontal scroll areas help showcase collections without forcing every item into a narrow grid."
                         Code="@HorizontalCode"
                         CodeLanguage="razor">
                <ScrollArea Class="w-96 rounded-md border whitespace-nowrap" ShowHorizontalScrollBar="true">
                    <Div Class="flex w-max space-x-4 p-4">
                        @foreach (var release in _releases)
                        {
                            <Div Class="w-40 shrink-0 rounded-lg border bg-card p-4">
                                <Paragraph TextSize="TextSize.Sm" FontWeight="FontWeight.Medium" Margin="Margin.Is0">@release.Title</Paragraph>
                                <Paragraph TextSize="TextSize.Sm" TextColor="TextColor.MutedForeground" Margin="Margin.Is0">@release.Description</Paragraph>
                            </Div>
                        }
                    </Div>
                </ScrollArea>
            </DemoSection>
        </Section>

        <Section Space="Space.Y.Is4">
            <H2 TextSize="TextSize.Xl" FontWeight="FontWeight.Semibold">Additional examples</H2>
            <Paragraph TextColor="TextColor.MutedForeground">
                Direction-aware usage beyond the shadcn-equivalent vertical and horizontal examples.
            </Paragraph>
        </Section>

        <Section Space="Space.Y.Is4">
            <DemoSection Title="RTL"
                         Description="Scroll areas can also present content naturally in right-to-left layouts."
                         Code="@RtlCode"
                         CodeLanguage="razor">
                <DirectionProvider Dir="rtl">
                    <ScrollArea Class="h-72 w-48 rounded-md border text-right">
                        <Div Class="p-4">
                            @for (var i = 0; i < _arabicNotes.Length; i++)
                            {
                                var index = i;

                                <Div Space="Space.Y.Is1">
                                    <Paragraph TextSize="TextSize.Sm" FontWeight="FontWeight.Medium" Margin="Margin.Is0">@_arabicNotes[index].Title</Paragraph>
                                    <Paragraph TextSize="TextSize.Sm" TextColor="TextColor.MutedForeground" Margin="Margin.Is0">@_arabicNotes[index].Description</Paragraph>
                                </Div>

                                @if (index < _arabicNotes.Length - 1)
                                {
                                    <Separator Class="my-4" />
                                }
                            }
                        </Div>
                    </ScrollArea>
                </DirectionProvider>
            </DemoSection>
        </Section>
    </Div>
</Div>

@code {
    private static readonly string[] _tags = Enumerable.Range(1, 50)
        .Select(index => $"v1.2.0-beta.{50 - index + 1}")
        .ToArray();

    private static readonly (string Title, string Description)[] _releases =
    [
        ("v3.2.0", "Sidebar improvements"),
        ("v3.1.4", "Dialog polish"),
        ("v3.1.0", "Command menu refresh"),
        ("v3.0.7", "Input group updates"),
        ("v3.0.0", "Navigation redesign")
    ];

    private static readonly (string Title, string Description)[] _arabicNotes =
    [
        ("المصادقة", "تمت إضافة حماية إعادة تشغيل الجلسة وتدوير السر الافتراضي للكوكيز."),
        ("الفوترة", "تم تحسين تذكيرات الفواتير وآليات إعادة المحاولة للبطاقات المنتهية."),
        ("عمليات النشر", "تم تبسيط الموافقات وإضافة ملخص إنتاج أكثر هدوءًا."),
        ("المراقبة", "تم توسيع تصفية السجلات لتقليل ضوضاء الوظائف الخلفية."),
        ("المحرر", "تم تقليل تكلفة العرض الأولي للجداول الكبيرة وقوائم الأوامر.")
    ];

    private const string VerticalCode =
@"<ScrollArea Class=""h-72 w-48 rounded-md border"">
    <Div Class=""p-4"">
        <H4 TextSize=""TextSize.Sm"" FontWeight=""FontWeight.Medium"" Class=""mb-4 leading-none"">Tags</H4>
        @foreach (var tag in tags)
        {
            <Div class=""text-sm"">@tag</Div>
            <Separator Class=""my-2"" />
        }
    </Div>
</ScrollArea>";

    private const string HorizontalCode =
@"<ScrollArea Class=""w-96 rounded-md border whitespace-nowrap"" ShowHorizontalScrollBar=""true"">
    <Div Class=""flex w-max space-x-4 p-4"">
        @foreach (var release in releases)
        {
            <Div Class=""w-40 shrink-0 rounded-lg border bg-card p-4"">...</Div>
        }
    </Div>
</ScrollArea>";

    private const string RtlCode =
@"<DirectionProvider Dir=""rtl"">
    <ScrollArea Class=""h-72 w-48 rounded-md border text-right"">
        <Div Class=""p-4"">
            @foreach (var note in arabicNotes)
            {
                <Div Class=""space-y-1"">...</Div>
                <Separator Class=""my-4"" />
            }
        </Div>
    </ScrollArea>
</DirectionProvider>";
}
