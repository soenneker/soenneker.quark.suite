@page "/calendars"

<PageTitle>Calendar - Quark Suite</PageTitle>

<Div Space="Space.Y.Is8" MaxWidth="MaxWidth.Is4xl">
    <Div Space="Space.Y.Is6">
        <Section Space="Space.Y.Is4">
            <DemoSection
                         Description="Single-date calendar with the month and year dropdown caption used in shadcn's calendar examples."
                         Code="@(@"<Calendar SelectedDate=""_basicDate"" SelectedDateChanged=""OnBasicDateChanged"" CaptionLayout=""dropdown"" Class=""rounded-lg border"" />")"
                         CodeLanguage="razor">
                <Calendar SelectedDate="_basicDate"
                          SelectedDateChanged="OnBasicDateChanged"
                          CaptionLayout="dropdown"
                          Class="rounded-lg border" />
            </DemoSection>
        </Section>

        <Section Space="Space.Y.Is4">
            <DemoSection Title="Basic"
                         Description="A basic calendar component styled with a rounded border."
                         Code="@(@"<Calendar SelectedDate=""_basicDate"" SelectedDateChanged=""OnBasicDateChanged"" Class=""rounded-lg border"" />")"
                         CodeLanguage="razor">
                <Calendar SelectedDate="_basicDate"
                          SelectedDateChanged="OnBasicDateChanged"
                          Class="rounded-lg border" />
            </DemoSection>
        </Section>

        <Section Space="Space.Y.Is4">
            <DemoSection Title="Range Calendar"
                         Description="Use range mode to enable range selection."
                         Code="@(@"<Card Class=""mx-auto w-fit p-0"">
    <CardBody Class=""p-0"">
        <Calendar Mode=""CalendarSelectionMode.Range"" DisplayMonth=""_rangeDisplayMonth"" SelectedRange=""_selectedRange"" SelectedRangeChanged=""OnSelectedRangeChanged"" NumberOfMonths=""2"" />
    </CardBody>
</Card>")"
                         CodeLanguage="razor">
                <Card Margin="Margin.Auto.OnX" Width="Width.IsFit" Padding="Padding.Is0">
                    <CardBody Padding="Padding.Is0">
                        <Calendar Mode="CalendarSelectionMode.Range"
                                  DisplayMonth="_rangeDisplayMonth"
                                  SelectedRange="_selectedRange"
                                  SelectedRangeChanged="OnSelectedRangeChanged"
                                  NumberOfMonths="2" />
                    </CardBody>
                </Card>
            </DemoSection>
        </Section>

        <Section Space="Space.Y.Is4">
            <DemoSection Title="Month and Year Selector"
                         Description="Use month and year dropdowns for direct caption navigation."
                         Code="@DropdownCaptionCode"
                         CodeLanguage="razor">
                <Card Margin="Margin.Auto.OnX" Width="Width.IsFit" Padding="Padding.Is0">
                    <CardBody Padding="Padding.Is0">
                        <Calendar SelectedDate="_dropdownCaptionDate"
                                  SelectedDateChanged="OnDropdownCaptionDateChanged"
                                  CaptionLayout="dropdown"
                                  CultureName="es-ES" />
                    </CardBody>
                </Card>
            </DemoSection>
        </Section>

        <Section Space="Space.Y.Is4">
            <DemoSection Title="Presets"
                         Description="Preset actions can drive the selected date while keeping the calendar as the source of date selection."
                         Code="@(@"<Div Class=""flex flex-col gap-3"">
    <Div Class=""flex gap-2"">
        <Button Variant=""ButtonVariant.Outline"" Size=""ButtonSize.Sm"" OnClick=""SetCalendarPresetToday"">Today</Button>
        <Button Variant=""ButtonVariant.Outline"" Size=""ButtonSize.Sm"" OnClick=""SetCalendarPresetNextWeek"">Next week</Button>
    </Div>
    <Calendar SelectedDate=""_presetDate"" SelectedDateChanged=""OnPresetDateChanged"" />
</Div>")"
                         CodeLanguage="razor">
                <Div Display="Display.Flex" FlexDirection="FlexDirection.Col" Gap="Gap.Is3" Width="Width.IsFit" Margin="Margin.Auto.OnX">
                    <Div Display="Display.Flex" Gap="Gap.Is2">
                        <Button Variant="ButtonVariant.Outline" Size="ButtonSize.Sm" OnClick="SetCalendarPresetToday">Today</Button>
                        <Button Variant="ButtonVariant.Outline" Size="ButtonSize.Sm" OnClick="SetCalendarPresetNextWeek">Next week</Button>
                    </Div>
                    <Calendar SelectedDate="_presetDate" SelectedDateChanged="OnPresetDateChanged" />
                </Div>
            </DemoSection>
        </Section>

        <Section Space="Space.Y.Is4">
            <DemoSection Title="Date and Time Picker"
                         Description="Use the date-time picker when the calendar selection needs a time value as well."
                         Code="@(@"<DateTimePicker @bind-SelectedDateTime=""_calendarDateTime"" />")"
                         CodeLanguage="razor">
                <Div Display="Display.Flex" FlexDirection="FlexDirection.Col" Gap="Gap.Is3" ItemsAlign="Items.Center">
                    <DateTimePicker @bind-SelectedDateTime="_calendarDateTime" />
                    <Paragraph Margin="Margin.Is0" TextSize="TextSize.Sm" TextColor="TextColor.MutedForeground">
                        @_calendarDateTime?.ToString("MMM dd, yyyy HH:mm")
                    </Paragraph>
                </Div>
            </DemoSection>
        </Section>

        <Section Space="Space.Y.Is4">
            <DemoSection Title="Booked dates"
                         Description="Use modifiers plus disabled dates to style and block already-booked days."
                         Code="@BookedDatesCode"
                         CodeLanguage="razor">
                <Card Margin="Margin.Auto.OnX" Width="Width.IsFit" Padding="Padding.Is0">
                    <CardBody Padding="Padding.Is0">
                        <Calendar SelectedDate="_bookedDate"
                                  SelectedDateChanged="OnBookedDateChanged"
                                  DisplayMonth="_bookedDisplayMonth"
                                  DisabledDates="_bookedDates"
                                  Modifiers="@BookedModifiers"
                                  ModifierClasses="@BookedModifierClasses" />
                    </CardBody>
                </Card>
            </DemoSection>
        </Section>

        <Section Space="Space.Y.Is4">
            <DemoSection Title="Custom Cell Size"
                         Description="Customize day cells with additional content such as pricing or availability cues."
                         Code="@CustomDaysCode"
                         CodeLanguage="razor">
                <Card Margin="Margin.Auto.OnX" Width="Width.IsFit" Padding="Padding.Is0">
                    <CardBody Padding="Padding.Is0">
                        <Calendar Mode="CalendarSelectionMode.Range"
                                  SelectedRange="_customDayRange"
                                  SelectedRangeChanged="OnCustomDayRangeChanged"
                                  CaptionLayout="dropdown"
                                  DayContent="@RenderPricingDay" />
                    </CardBody>
                </Card>
            </DemoSection>
        </Section>

        <Section Space="Space.Y.Is4">
            <DemoSection Title="Week Numbers"
                         Description="Use week numbers for scheduling and planning workflows."
                         Code="@(@"<Card Class=""mx-auto w-fit p-0"">
    <CardBody Class=""p-0"">
        <Calendar SelectedDate=""_weekNumberDate"" SelectedDateChanged=""OnWeekNumberDateChanged"" ShowWeekNumber=""true"" />
    </CardBody>
</Card>")"
                         CodeLanguage="razor">
                <Card Margin="Margin.Auto.OnX" Width="Width.IsFit" Padding="Padding.Is0">
                    <CardBody Padding="Padding.Is0">
                        <Calendar SelectedDate="_weekNumberDate"
                                  SelectedDateChanged="OnWeekNumberDateChanged"
                                  ShowWeekNumber="true" />
                    </CardBody>
                </Card>
            </DemoSection>
        </Section>

        <Section Space="Space.Y.Is4">
            <DemoSection Title="RTL"
                         Description="Calendar supports right-to-left layouts with locale-aware formatting."
                         Code="@(@"<DirectionProvider Dir=""rtl"">
    <Calendar SelectedDate=""_rtlDate"" SelectedDateChanged=""OnRtlDateChanged"" CultureName=""ar-SA"" Class=""rounded-lg border"" />
</DirectionProvider>")"
                         CodeLanguage="razor">
                <DirectionProvider Dir="rtl">
                    <Calendar SelectedDate="_rtlDate"
                              SelectedDateChanged="OnRtlDateChanged"
                              CultureName="ar-SA"
                              Class="rounded-lg border" />
                </DirectionProvider>
            </DemoSection>
        </Section>

        <Section Space="Space.Y.Is4">
            <DemoSection Title="Multiple Months"
                         Description="Increase `NumberOfMonths` to present a broader range-selection surface."
                         Code="@(@"<Card Class=""mx-auto w-fit p-0"">
    <CardBody Class=""p-0"">
        <Calendar Mode=""CalendarSelectionMode.Range"" DisplayMonth=""_multiMonthDisplayMonth"" SelectedRange=""_multiMonthRange"" SelectedRangeChanged=""OnMultiMonthRangeChanged"" NumberOfMonths=""3"" CultureName=""es-ES"" />
    </CardBody>
</Card>")"
                         CodeLanguage="razor">
                <Card Margin="Margin.Auto.OnX" Width="Width.IsFit" Padding="Padding.Is0">
                    <CardBody Padding="Padding.Is0">
                        <Calendar Mode="CalendarSelectionMode.Range"
                                  DisplayMonth="_multiMonthDisplayMonth"
                                  SelectedRange="_multiMonthRange"
                                  SelectedRangeChanged="OnMultiMonthRangeChanged"
                                  NumberOfMonths="3"
                                  CultureName="es-ES" />
                    </CardBody>
                </Card>
            </DemoSection>
        </Section>

        <Section Space="Space.Y.Is4">
            <DemoSection Title="Date Bounds"
                         Description="Bounds work well for birth dates and other historical-only selections."
                         Code="@(@"<Calendar SelectedDate=""_birthday"" SelectedDateChanged=""OnBirthdayChanged"" Min=""_birthdayMin"" Max=""_birthdayMax"" />")"
                         CodeLanguage="razor">
                <Div Display="Display.Flex" Flex=@("flex-col") ItemsAlign="Items.Start" Gap="Gap.Is4">
                    <Calendar SelectedDate="_birthday"
                              SelectedDateChanged="OnBirthdayChanged"
                              Min="_birthdayMin"
                              Max="_birthdayMax" />
                    <Paragraph TextSize="TextSize.Sm" TextColor="TextColor.MutedForeground">
                        Choose a date between @_birthdayMin.ToString("yyyy-MM-dd") and @_birthdayMax.ToString("yyyy-MM-dd")
                    </Paragraph>
                </Div>
            </DemoSection>
        </Section>

        <Section Space="Space.Y.Is4">
            <DemoSection Title="Hide Outside Days"
                         Description="Disable outside days when the current month should stay visually focused."
                         Code="@(@"<Calendar SelectedDate=""_focusedMonthDate"" SelectedDateChanged=""OnFocusedMonthDateChanged"" ShowOutsideDays=""false"" />")"
                         CodeLanguage="razor">
                <Div Display="Display.Flex" Flex=@("flex-col") ItemsAlign="Items.Start" Gap="Gap.Is4">
                    <Calendar SelectedDate="_focusedMonthDate"
                              SelectedDateChanged="OnFocusedMonthDateChanged"
                              ShowOutsideDays="false" />
                    <Paragraph TextSize="TextSize.Sm" TextColor="TextColor.MutedForeground">
                        Outside days are hidden while the month grid stays aligned.
                    </Paragraph>
                </Div>
            </DemoSection>
        </Section>
    </Div>
</Div>

@code {
    private DateOnly? _basicDate = DateOnly.FromDateTime(DateTime.Today);
    private CalendarDateRange? _selectedRange = new(DateOnly.FromDateTime(DateTime.Today.AddDays(-30)), DateOnly.FromDateTime(DateTime.Today.AddDays(-10)));
    private DateOnly[] _selectedDates =
    [
        DateOnly.FromDateTime(DateTime.Today),
        DateOnly.FromDateTime(DateTime.Today.AddDays(2)),
        DateOnly.FromDateTime(DateTime.Today.AddDays(5))
    ];
    private DateOnly? _weekNumberDate = DateOnly.FromDateTime(DateTime.Today.AddDays(10));
    private CalendarDateRange? _multiMonthRange = new(DateOnly.FromDateTime(DateTime.Today.AddDays(12)), DateOnly.FromDateTime(DateTime.Today.AddDays(42)));
    private DateOnly? _dropdownCaptionDate = DateOnly.FromDateTime(DateTime.Today.AddDays(14));
    private DateOnly? _presetDate = DateOnly.FromDateTime(DateTime.Today);
    private DateTime? _calendarDateTime = DateTime.Today.AddHours(9);
    private readonly DateOnly _bookedDisplayMonth = new(DateTime.Today.Year, DateTime.Today.Month, 1);
    private DateOnly[] _bookedDates =
    [
        DateOnly.FromDateTime(DateTime.Today.AddDays(3)),
        DateOnly.FromDateTime(DateTime.Today.AddDays(4)),
        DateOnly.FromDateTime(DateTime.Today.AddDays(8)),
        DateOnly.FromDateTime(DateTime.Today.AddDays(9))
    ];
    private DateOnly? _bookedDate = DateOnly.FromDateTime(DateTime.Today.AddDays(12));
    private CalendarDateRange? _customDayRange = new(DateOnly.FromDateTime(DateTime.Today.AddDays(20)), DateOnly.FromDateTime(DateTime.Today.AddDays(27)));
    private readonly DateOnly _birthdayMin = new(1940, 1, 1);
    private readonly DateOnly _birthdayMax = DateOnly.FromDateTime(DateTime.Today);
    private DateOnly? _birthday = new(1992, 4, 12);
    private DateOnly? _focusedMonthDate = DateOnly.FromDateTime(DateTime.Today.AddDays(3));
    private DateOnly? _rtlDate = DateOnly.FromDateTime(DateTime.Today);

    private Dictionary<string, Func<DateOnly, bool>> BookedModifiers => new()
    {
        ["booked"] = date => _bookedDates.Contains(date)
    };

    private static Dictionary<string, string> BookedModifierClasses => new()
    {
        ["booked"] = "[&>button]:line-through opacity-100"
    };

    private DateOnly? _rangeDisplayMonth => _selectedRange?.From;
    private DateOnly? _multiMonthDisplayMonth => _multiMonthRange?.From;

    private Task OnBasicDateChanged(DateOnly? value)
    {
        _basicDate = value;
        return Task.CompletedTask;
    }

    private Task OnBirthdayChanged(DateOnly? value)
    {
        _birthday = value;
        return Task.CompletedTask;
    }

    private Task OnSelectedRangeChanged(CalendarDateRange? value)
    {
        _selectedRange = value;
        return Task.CompletedTask;
    }

    private Task OnSelectedDatesChanged(DateOnly[] value)
    {
        _selectedDates = value;
        return Task.CompletedTask;
    }

    private Task OnPresetDateChanged(DateOnly? value)
    {
        _presetDate = value;
        return Task.CompletedTask;
    }

    private void SetCalendarPresetToday()
    {
        _presetDate = DateOnly.FromDateTime(DateTime.Today);
    }

    private void SetCalendarPresetNextWeek()
    {
        _presetDate = DateOnly.FromDateTime(DateTime.Today.AddDays(7));
    }

    private Task OnWeekNumberDateChanged(DateOnly? value)
    {
        _weekNumberDate = value;
        return Task.CompletedTask;
    }

    private Task OnMultiMonthRangeChanged(CalendarDateRange? value)
    {
        _multiMonthRange = value;
        return Task.CompletedTask;
    }

    private Task OnDropdownCaptionDateChanged(DateOnly? value)
    {
        _dropdownCaptionDate = value;
        return Task.CompletedTask;
    }

    private Task OnBookedDateChanged(DateOnly? value)
    {
        _bookedDate = value;
        return Task.CompletedTask;
    }

    private Task OnCustomDayRangeChanged(CalendarDateRange? value)
    {
        _customDayRange = value;
        return Task.CompletedTask;
    }

    private Task OnFocusedMonthDateChanged(DateOnly? value)
    {
        _focusedMonthDate = value;
        return Task.CompletedTask;
    }

    private Task OnRtlDateChanged(DateOnly? value)
    {
        _rtlDate = value;
        return Task.CompletedTask;
    }

    private static string FormatRange(CalendarDateRange? value)
    {
        if (value?.From is null)
            return "No dates selected";

        if (value.To is null)
            return value.From.Value.ToString("yyyy-MM-dd");

        return $"{value.From.Value:yyyy-MM-dd} to {value.To.Value:yyyy-MM-dd}";
    }

    private static string FormatDates(IEnumerable<DateOnly> values)
    {
        var selected = values.Select(value => value.ToString("yyyy-MM-dd")).ToArray();
        return selected.Length == 0 ? "No dates selected" : string.Join(", ", selected);
    }

    private static bool IsWeekend(DateOnly date)
    {
        return date.DayOfWeek is DayOfWeek.Saturday or DayOfWeek.Sunday;
    }

    private RenderFragment<CalendarDayContext> RenderPricingDay => context => @<Div Display="Display.Flex" Flex=@("flex-col") ItemsAlign="Items.Center" Leading="Leading.None">
        <Span>@context.Date.Day</Span>
        @if (context.InMonth)
        {
            <Span TextSize='TextSize.Token("[10px]")' Class="opacity-70">@(IsWeekend(context.Date) ? "$120" : "$100")</Span>
        }
    </Div>;

    private const string DropdownCaptionCode =
@"<Card Class=""mx-auto w-fit p-0"">
    <CardBody Class=""p-0"">
        <Calendar SelectedDate=""selectedDate"" SelectedDateChanged=""OnDateChanged"" CaptionLayout=""dropdown"" CultureName=""es-ES"" />
    </CardBody>
</Card>";

    private const string BookedDatesCode =
@"<Card Class=""mx-auto w-fit p-0"">
    <CardBody Class=""p-0"">
        <Calendar SelectedDate=""selectedDate""
                  DisplayMonth=""bookedDisplayMonth""
                  DisabledDates=""bookedDates""
                  Modifiers=""BookedModifiers""
                  ModifierClasses=""BookedModifierClasses"" />
    </CardBody>
</Card>";

    private const string CustomDaysCode =
@"<Card Class=""mx-auto w-fit p-0"">
    <CardBody Class=""p-0"">
        <Calendar Mode=""CalendarSelectionMode.Range""
                  SelectedRange=""selectedRange""
                  SelectedRangeChanged=""OnRangeChanged""
                  CaptionLayout=""dropdown""
                  DayContent=""RenderPricingDay"" />
    </CardBody>
</Card>";
}
